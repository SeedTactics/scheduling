name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1
      - uses: actions/setup-dotnet@v1
        with:
          dotnet-version: "3.1.101"
      - run: dotnet build BlackMaple.SeedTactics.Scheduling
      - run: dotnet build allocatecli
